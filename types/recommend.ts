export interface CommentRecommend {
  id: string;
  isAlready: boolean;
  isPlus: boolean;
  isReply: boolean;
}

export interface PostRecommend {
  id: string;
  isAlready: boolean;
  isPost: boolean;
}
